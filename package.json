{
  "name": "cavero-watches",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build-seo.cjs && node scripts/finalize-seo.cjs",
    "test": "node --test"
  },
  "dependencies": {
    "stripe": "^18.0.0"
  }
}
